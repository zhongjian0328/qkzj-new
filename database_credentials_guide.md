# 阿里云MongoDB实例用户名和密码获取指南

## 一、查看现有数据库账号

1. **在当前实例页面左侧导航栏**，点击 **「账号管理」** 选项
2. **在账号管理页面**，你可以查看所有已创建的数据库账号
3. **找到需要的账号**，可以看到账号名称和账号类型

## 二、重置数据库密码

如果忘记了密码或需要修改密码：

1. **在账号管理页面**，找到需要重置密码的账号
2. **点击账号名称右侧的「重置密码」按钮**
3. **在弹出的对话框中**，输入新密码
4. **点击「确定」** 完成密码重置

## 三、创建新的数据库账号

1. **在账号管理页面**，点击 **「创建账号」** 按钮
2. **填写账号信息**：
   - 账号名称：如 `power-application-user`
   - 密码：设置强密码，如 `<your-strong-password>`
   - 账号类型：根据需要选择（普通账号或超级账号）
   - 权限设置：为账号分配相应的数据库权限
3. **点击「确定」** 完成账号创建

## 四、查看连接字符串

在当前实例的 **「基本信息」** 页面，你可以看到 **「连接信息」** 部分：

- **公网连接字符串**：用于从公网访问数据库
- **专有网络连接字符串**：用于在阿里云VPC内访问数据库

连接字符串格式为：
```
mongodb://用户名:<your-strong-password>@主机地址1:端口1,主机地址2:端口2/数据库名?replicaSet=副本集名称
```

## 五、使用建议

1. **确保密码安全**：使用强密码，定期更换
2. **权限最小化原则**：为账号分配最小必要的权限
3. **使用专有网络**：在生产环境中，建议使用专有网络连接
4. **定期备份**：定期备份数据库，确保数据安全
5. **监控实例状态**：关注实例的运行状态和性能指标

## 六、常见问题

### 1. 无法连接数据库
- 检查网络连接是否正常
- 检查用户名和密码是否正确
- 检查数据库实例的白名单设置
- 检查实例是否处于运行状态

### 2. 忘记密码
- 在账号管理页面重置密码

### 3. 权限不足
- 在账号管理页面修改账号权限

## 七、注意事项

- 数据库账号密码是重要的敏感信息，妥善保管
- 不要将密码泄露给无关人员
- 定期更换密码，提高安全性
- 确保连接字符串中的用户名和密码没有多余空格或特殊字符