# 开发预览环境使用指南

## 1. 环境概览

### 1.1 服务状态
- **后端服务**: 运行在 http://localhost:3000，支持热重载
- **前端服务**: 运行在 http://localhost:8081 (Web) 和 exp://192.168.124.10:8081 (移动设备)
- **MongoDB**: 本地数据库，连接正常
- **AI服务**: 已初始化，百度云API不可用时自动降级到模拟模式

### 1.2 技术栈
- **后端**: Node.js, Express.js, MongoDB
- **前端**: React Native, Expo
- **AI服务**: 百度云图像识别API (带模拟降级)

## 2. 访问方式

### 2.1 Web版本访问
- **地址**: http://localhost:8081
- **功能**: 完整的Web端界面，支持热重载
- **适用场景**: 开发调试、快速测试

### 2.2 移动设备访问
- **地址**: exp://192.168.124.10:8081
- **访问方式**: 使用Expo Go应用扫描QR码
- **适用场景**: 移动设备兼容性测试、真实设备体验

### 2.3 API端点访问
- **基础URL**: http://localhost:3000/api
- **健康检查**: http://localhost:3000/api/health
- **AI诊断健康检查**: http://localhost:3000/api/ai-diagnosis/health

## 3. 开发流程

### 3.1 启动服务

#### 后端服务启动
```bash
# 进入后端目录
cd backend

# 启动服务（支持热重载）
npm start
```

**说明**：后端服务已配置 `nodemon` 支持热重载，代码修改后会自动重启服务器，无需手动停止和启动。

#### 前端服务启动
```bash
# 在项目根目录
npm start
```

### 3.2 热重载功能
- **Web版本**: 代码修改后自动刷新
- **移动版本**: 代码修改后按 `r` 键刷新，或在Expo Go应用中下拉刷新

### 3.3 开发工具
- **Metro Bundler**: 前端资源打包
- **Expo DevTools**: 开发调试工具，可通过 http://localhost:19002 访问

## 4. 功能测试

### 4.1 API测试

#### 健康检查
```bash
# 测试后端服务
curl http://localhost:3000/api/health

# 测试AI诊断服务
curl http://localhost:3000/api/ai-diagnosis/health
```

#### AI功能测试
- **对话问诊**: POST /api/ai-diagnosis/chat-diagnosis
- **兽医模式诊断**: POST /api/ai-diagnosis/veterinary-diagnosis
- **混合感染风险评估**: POST /api/ai-diagnosis/risk-assessment
- **紧急控制方案**: POST /api/ai-diagnosis/emergency-plan

### 4.2 前端功能测试
- **登录/注册**: 测试用户认证流程
- **AI诊断模式选择**: 测试模式切换功能
- **对话问诊**: 测试聊天界面和AI响应
- **兽医诊断**: 测试表单提交和AI分析
- **生产管理**: 测试批次管理和数据记录

## 5. 环境配置

### 5.1 后端配置
- **端口**: 3000
- **数据库**: MongoDB本地连接
- **AI服务**: 百度云API (带模拟降级)
- **超时设置**: 60秒

### 5.2 前端配置
- **API基础URL**: http://localhost:3000/api
- **超时设置**: 65秒
- **热重载**: 启用

### 5.3 网络设置
- **防火墙**: 确保8081和3000端口可访问
- **局域网访问**: 移动设备需与开发机在同一局域网
- **DNS解析**: 确保localhost解析正常

## 6. 常见问题排查

### 6.1 后端服务问题
- **MongoDB连接失败**: 检查MongoDB服务是否启动
- **端口占用**: 检查3000端口是否被占用
- **AI服务错误**: 检查百度云API密钥配置

### 6.2 前端服务问题
- **Expo启动失败**: 检查依赖是否安装完整
- **Web访问错误**: 检查8081端口是否被占用
- **热重载失效**: 检查文件保存是否成功

### 6.3 网络问题
- **移动设备无法访问**: 检查局域网连接和防火墙设置
- **API调用失败**: 检查前后端服务是否都在运行
- **超时错误**: 检查网络连接速度和稳定性

## 7. 开发最佳实践

### 7.1 代码规范
- **前端**: 使用TypeScript，遵循ESLint规则
- **后端**: 使用JavaScript，遵循ESLint规则
- **提交信息**: 清晰描述修改内容

### 7.2 测试策略
- **单元测试**: 测试核心功能模块
- **集成测试**: 测试前后端交互
- **端到端测试**: 测试完整用户流程

### 7.3 性能优化
- **代码分割**: 前端使用动态导入
- **缓存策略**: 合理使用后端缓存
- **网络优化**: 减少不必要的API调用

## 8. 部署准备

### 8.1 环境变量配置
- **开发环境**: 使用 `.env` 文件
- **生产环境**: 使用环境变量或密钥管理服务

### 8.2 构建流程
```bash
# 前端Web构建
npm run build:web

# 前端移动构建
eas build --platform android
eas build --platform ios

# 后端构建
# 无需特殊构建，直接部署
```

### 8.3 发布流程
1. 代码审查和测试
2. 构建生产版本
3. 部署到测试环境
4. 验证功能正常
5. 部署到生产环境

## 9. 联系方式

### 9.1 技术支持
- **问题反馈**: 通过项目issue或邮件
- **技术讨论**: 团队会议或在线讨论

### 9.2 文档更新
- **文档维护**: 代码变更时同步更新文档
- **版本控制**: 文档与代码保持同步版本

---

**注意**: 开发预览环境仅供开发和测试使用，请勿用于生产环境。所有敏感信息（如API密钥）应通过环境变量配置，不得硬编码在代码中。